name: Trigger Vercel deploy on main

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        env:
          VERCEL_HOOK_MAIN: ${{ secrets.VERCEL_HOOK_MAIN }}
        run: |
          curl -fsSL "$VERCEL_HOOK_MAIN"
