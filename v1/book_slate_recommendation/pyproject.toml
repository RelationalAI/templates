[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-book-slate-recommendation"
version = "0.0.0"
description = "RelationalAI template: book_slate_recommendation (PyRel v1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai==1.1.0",
  "pandas>=2.0.0",
]

[tool.setuptools]
packages = []
