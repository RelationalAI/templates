[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-shipment-compliance"
version = "0.0.0"
description = "RelationalAI template: shipment_compliance (PyRel v1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai==1.0.14",
  "pandas",
]

[tool.setuptools]
packages = []
