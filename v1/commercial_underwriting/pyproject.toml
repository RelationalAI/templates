[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-commercial-underwriting"
version = "0.0.0"
description = "RelationalAI template: commercial_underwriting (PyRel v1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai==1.11.0",
  "pandas",
]

[tool.setuptools]
packages = []
