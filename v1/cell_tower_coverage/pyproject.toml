[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-cell-tower-coverage"
version = "0.0.0"
description = "RelationalAI template: cell_tower_coverage (PyRel v1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai==1.0.14",
  "pandas>=2.0",
]

[tool.setuptools]
packages = []
