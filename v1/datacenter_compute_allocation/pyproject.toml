[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-datacenter-compute-allocation"
version = "0.0.0"
description = "RelationalAI template: datacenter_compute_allocation (PyRel v1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai==1.2.2",
  "pandas>=2.0",
]

[tool.setuptools]
packages = []
