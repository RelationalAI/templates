[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-patient-cohort-recruitment"
version = "0.0.0"
description = "RelationalAI template: patient_cohort_recruitment (PyRel v1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai==1.1.1",
  "pandas",
]

[tool.setuptools]
packages = []
