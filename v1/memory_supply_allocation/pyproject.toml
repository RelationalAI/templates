[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-memory-supply-allocation"
version = "0.0.0"
description = "RelationalAI template: memory_supply_allocation (PyRel v1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai[gnn]==1.5.0",
  "pandas",
]

[tool.setuptools]
packages = []
