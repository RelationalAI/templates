[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-machine-dependencies"
version = "0.0.0"
description = "RelationalAI template: machine dependency graph analysis via shared technician qualifications"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai==1.0.3",
  "pandas",
]

[tool.setuptools]
packages = []
