[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-supplier-impact-analysis"
version = "0.0.0"
description = "RelationalAI template: supplier disruption impact analysis with multi-hop reachability on business networks"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai==1.0.14",
  "pandas>=2.0.0",
]

[tool.setuptools]
packages = []
