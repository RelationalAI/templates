[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-telco-network-recovery"
version = "0.0.0"
description = "RelationalAI template: telco_network_recovery (PyRel v1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai[gnn]==1.27.1",
  "pandas>=2.0",
]

[tool.setuptools]
packages = []
