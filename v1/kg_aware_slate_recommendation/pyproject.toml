[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rai-template-kg-aware-slate-recommendation"
version = "0.0.0"
description = "RelationalAI template: kg_aware_slate_recommendation (PyRel v1)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "relationalai==1.1.0",
  "pandas>=2.0.0",
]

[tool.setuptools]
packages = []
